# Premier League Football Predictor

Observable Framework site for the prediction model. Data feed lives in
`src/data/*.json`, regenerated by `python ../src/export_web.py` (and by CI).

```
npm run dev      # local preview
npm run build    # static build -> dist/
```
